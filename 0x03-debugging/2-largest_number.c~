#include "main.h"

/**
* largest_number - return largest of 3 numbers
* @a: first integer
* @b: second integer
* @c: third integer
* Return: largest
*/

int largest_number(int a, int b, int c)
{
int largest;
of (a >= b && a >= c)
